global:
  scrape_interval: 15s     # How often to fetch metrics from targets
  evaluation_interval: 15s # How often to evaluate alerting rules

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['inference:8000']